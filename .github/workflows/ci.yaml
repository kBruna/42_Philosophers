name: ci

on:
    push:
        branches: ["main"]
    pull_request:
        branches: ["main"]

jobs:
  norminette:
    runs-on: ubuntu-latest
    steps:
       - uses: actions/checkout@v6
       - uses: 42school/norminette-action@v3
         with:
          args: '-CheckForbiddenSourceHeader'

  tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: compile philo
        run: cd philo && make
      - name: run tests
        run: cd philo && make unit-tests
